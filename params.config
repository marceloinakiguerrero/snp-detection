// Define parameters in main.nf

params{

    //Input paths
    fasta_dir = "$launchDir/fasta_files/*.fa"
    reads_dir = "$launchDir/reads_files/"

    //Output Paths
    intermediates_dir = "$launchDir/intermediate_files"
    results_dir = "$launchDir/"
}